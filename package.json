{
  "name": "mv-music",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "MV Music — Cloudflare Workers music app with Audius + Jamendo provider layer.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
